# Domain Docs

This is a single-context repository.

Before engineering work, read the root `CONTEXT.md` and any relevant records in `docs/adr/`.

Use the vocabulary defined in `CONTEXT.md` for issue titles, specifications, designs, and tests.

Proceed silently when optional ADR documentation does not exist.
